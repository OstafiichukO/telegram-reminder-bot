# Render Blueprint specification
services:
  - type: web
    name: telegram-reminder-bot
    runtime: docker
    plan: free
    envVars:
      - key: BOT_TOKEN
        sync: false  # You'll set this manually in Render dashboard
      - key: OPENAI_API_KEY
        sync: false  # Optional: for ChatGPT integration
