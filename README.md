# Telegram Reminder Bot 🔔

Телеграм бот для створення та управління нагадуваннями з підтримкою повторюваних нагадувань.

## Можливості

- ✅ Створення нагадувань з назвою та часом
- 🔄 Підтримка повторюваних нагадувань (щогодини, щодня, щотижня, щомісяця)
- 📋 Перегляд списку активних нагадувань
- 🗑 Видалення нагадувань
- 💾 Збереження даних у SQLite базі

## Встановлення

### 1. Клонування репозиторію

```bash
cd ~/GitHub/telegram-reminder-bot
```

### 2. Створення віртуального середовища

```bash
python3 -m venv venv
source venv/bin/activate  # Linux/macOS
# або
venv\Scripts\activate  # Windows
```

### 3. Встановлення залежностей

```bash
pip install -r requirements.txt
```

### 4. Налаштування токена бота

1. Відкрийте Telegram і знайдіть [@BotFather](https://t.me/BotFather)
2. Надішліть `/newbot` та слідуйте інструкціям
3. Скопіюйте отриманий токен
4. Створіть файл `.env`:

```bash
cp .env.example .env
```

5. Відкрийте `.env` і вставте ваш токен:

```
BOT_TOKEN=your_bot_token_here
```

### 5. Запуск бота

```bash
python bot.py
```

## Команди бота

| Команда | Опис |
|---------|------|
| `/start` | Привітання та інформація про бота |
| `/add` | Створити нове нагадування |
| `/list` | Переглянути всі активні нагадування |
| `/delete` | Видалити нагадування |
| `/help` | Показати довідку |
| `/cancel` | Скасувати поточну дію |

## Формат часу

При створенні нагадування можна використовувати:

- **Повний формат:** `ДД.ММ.РРРР ГГ:ХХ` (наприклад: `25.12.2025 14:30`)
- **Тільки час:** `ГГ:ХХ` (наприклад: `14:30`) - буде встановлено на сьогодні або завтра

## Типи повторення

- **Один раз** - нагадування спрацює лише один раз
- **Щогодини** - повторюється кожну годину
- **Щодня** - повторюється кожен день о тій самій годині
- **Щотижня** - повторюється кожен тиждень
- **Щомісяця** - повторюється приблизно кожен місяць (30 днів)

## Структура проєкту

```
telegram-reminder-bot/
├── bot.py           # Головний файл бота
├── database.py      # Робота з базою даних
├── scheduler.py     # Планувальник нагадувань
├── requirements.txt # Залежності
├── .env.example     # Приклад конфігурації
├── .env             # Конфігурація (не в git)
├── .gitignore       # Ігноровані файли
└── README.md        # Документація
```

## Технології

- [python-telegram-bot](https://python-telegram-bot.org/) - бібліотека для Telegram Bot API
- [APScheduler](https://apscheduler.readthedocs.io/) - планувальник задач
- [SQLite](https://www.sqlite.org/) - база даних
- [python-dotenv](https://pypi.org/project/python-dotenv/) - робота з .env файлами

## Ліцензія

MIT
